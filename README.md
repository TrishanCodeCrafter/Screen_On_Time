Requirements:
VSCode
Python

clone this repo

open terminal in IDE

(you can create a virtual environment if you want...)

pip install -r requirements.txt

to run the application > python main.py

and keep the program running , the terminal will show SOT , Battery % and SOT estimate
